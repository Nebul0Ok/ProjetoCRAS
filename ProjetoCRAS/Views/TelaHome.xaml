<UserControl x:Class="ProjetoCRAS.Views.TelaHome"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjetoCRAS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
    <!--Botão do Menu-->
    <!--#region -->
        <Style x:Key="BotoesMenu" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>

            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>

            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0.2,0" EndPoint="1.3,1">
                        <GradientStop Color="#4aa2d0" Offset="0"/>
                        <GradientStop Color="#327ab3" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#F3FF59"/>


            <Setter Property="HorizontalContentAlignment" Value="Left"/>

            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                        CornerRadius="10" 
                        BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" 
                               Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                               Width="40" Height="40" 
                               Margin="10,0,5,0"
                               VerticalAlignment="Center"/>

                                <ContentPresenter Grid.Column="1" 
                                          HorizontalAlignment="Right" 
                                          VerticalAlignment="Center" 
                                          Margin="0,0,15,0"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                     To="1.1" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                     To="1.1" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>

                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                     To="1.0" Duration="0:0:0.2"/>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                     To="1.0" Duration="0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
            

        </Style> 
        <!--#endregion-->
    </UserControl.Resources>


    <Grid>
        <!--<ContentControl x:Name="ContentMenuLateral" Panel.ZIndex="10"/>-->
        
        <Grid Background="#3a97c9">



            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="90*"/>
            </Grid.RowDefinitions>

            <!--Barra Superior-->
            <!--region-->
            <Grid Grid.Row="0" Background="#327ab3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="{StaticResource IconeMenu}" HorizontalAlignment="Left" Margin="10"/>
                <Button x:Name="BtnMenuLateral" Click="BtnMenuLateral_Click" 
                Grid.Column="0" Background="Transparent" BorderBrush="Transparent"
                Width="28" HorizontalAlignment="Left" Margin="10"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Nome_Usuario" Foreground="#F3FF59"  VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0,0,10,0"/>

                    <Grid>
                        <Button Width="45" Height="45" Background="Transparent"/>
                        <Image Source="{StaticResource Usuario}"/>
                    </Grid>
                </StackPanel>
            </Grid>
            <!--#endregion-->

            <!--Menu-->
            <!--#region-->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70*"/>
                    <ColumnDefinition Width="30*"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Button Grid.Column="0" x:Name="BtnEstoque" Click="BtnEstoque_Click"
                    Grid.Row="0" Width="200" 
                    Height="100" Content="Estoque"
                    Style="{StaticResource BotoesMenu}"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Margin="15"
                    Tag="{StaticResource Caixa}"
                    />

                    <Button Grid.Column="1" x:Name="BtnDeposito" Click="BtnDeposito_Click"
                    Grid.Row="0" Width="200" 
                    Height="100" Content="Depositos"
                    Style="{StaticResource BotoesMenu}"
                    HorizontalAlignment="Left" VerticalAlignment="Bottom"
                    Margin="15"/>

                    <Button Grid.Column="0" x:Name="BtnItens" Click="BtnItens_Click"
                    Grid.Row="1" Width="200" 
                    Height="100" Content="Itens"
                    Style="{StaticResource BotoesMenu}"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="15"/>

                    <Button Grid.Column="1" x:Name="BtnHistorico" Click="BtnHistorico_Click"
                    Grid.Row="1" Width="200" 
                    Height="100" Content="Histórico"
                    Style="{StaticResource BotoesMenu}"
                    HorizontalAlignment="Left" VerticalAlignment="Top"
                    Margin="15"/>

                </Grid>

                <Image Grid.Column="1" Source="/Source/Cras_Logo.png" Width="200" Height="200"/>

            </Grid>
            <!--#endregion-->
        </Grid>
    </Grid>

</UserControl>
