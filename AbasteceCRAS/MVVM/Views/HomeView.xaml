<UserControl x:Class="AbasteceCRAS.MVVM.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AbasteceCRAS.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10,0,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock Text="Bem-vindo ao Sistema de Gestão CRAS" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Selecione uma opção para continuar" Margin="0,0,0,20"/>

            <UniformGrid Rows="2" Columns="2" Width="500" Height="300" Margin="0,0,0,0">
                <Button Style="{StaticResource CardButtonStyle}" Content="Estoque"/>
                <Button Style="{StaticResource CardButtonStyle}" Content="Depósitos"/>
                <Button Style="{StaticResource CardButtonStyle}" Content="Itens"/>
                <Button Style="{StaticResource CardButtonStyle}" Content="Histórico"/>
            </UniformGrid>
        </StackPanel>

        <Image Grid.Column="1" Source="/img/logo_cras.svg" Width="300" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
