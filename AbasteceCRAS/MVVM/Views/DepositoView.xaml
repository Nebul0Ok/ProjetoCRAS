<UserControl x:Class="AbasteceCRAS.MVVM.Views.DepositoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AbasteceCRAS.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <UserControl.Resources>
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="#F0F4F8"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </UserControl.Resources>


    <Grid Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="0.8*"/>
            <RowDefinition Height="9.9*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Margin="10"
            Content="Voltar" Command="{Binding RetornarParaHome}"/>


        <Grid Grid.Row="1" Grid.Column="1" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" Margin="60,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,40,0">
                    <TextBlock Text="Gerenciamento de Depósitos" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>

                    <Border Background="#F9F9F9" Padding="25" CornerRadius="15">
                        <StackPanel>
                            <TextBlock Text="Adicionar Novo Depósito" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

                            <TextBlock Text="Nome do Depósito*" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding NomeInput, UpdateSourceTrigger=PropertyChanged}" 
                Height="40" Margin="0,0,0,20" VerticalContentAlignment="Center" Padding="10,0"/>

                            <TextBlock Text="Localização" Style="{StaticResource FieldLabelStyle}"/>
                            <TextBox Text="{Binding LocalInput, UpdateSourceTrigger=PropertyChanged}" 
                Height="40" Margin="0,0,0,20" VerticalContentAlignment="Center" Padding="10,0"/>


                            <Button Content="+ Adicionar" 
                    Background="Black" 
                    Foreground="White" 
                    Height="45" 
                    FontWeight="Bold"
                    FontSize="14"
                    Cursor="Hand"
                    Command="{Binding AdicionarDepositoCommand}">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1">

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,25">
                        <TextBlock FontSize="22" FontWeight="Bold" Margin="0,10,0,25">
                <Run Text="Depósitos Cadastrados ("/>
                <Run Text="{Binding ListaDepositos.Count, Mode=OneWay}"/>
                <Run Text=")"/>
                        </TextBlock>
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding ListaDepositos}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                        CornerRadius="15" Margin="0,0,0,15" Padding="20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Width="60" Height="60" Background="#F0F4F8" CornerRadius="10">
                                            <TextBlock Text="🏠" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="20,0">

                                            <TextBlock Text="{Binding Nome}" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                                            <TextBlock Text="{Binding Localizacao}" FontSize="14" Foreground="#666" Margin="0,2,0,5"/>

                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </Grid>


    </Grid>


    
</UserControl>
